package com.example.tablelink.filter;

public enum FilterOperator {
    EQ,
    NEQ,
    GT,
    GTE,
    LT,
    LTE,
    LIKE,
    IS_NULL,
    IS_NOT_NULL,
    WITHIN_LAST_N_DAYS,
    OLDER_THAN_N_DAYS
}
